---
layout: base
pagination:
  data: static.userConfirmSuccesses
  size: 1
  alias: item
  addAllPagesToCollections: true
permalink: "{{ item.permalink }}"
noindex: true
eleventyComputed:
  key: "{{ item.key }}"
  locale: "{{ item.locale }}"
  title: "{{ item.title | safe }}"
  description: "{{ item.description | safe }}"
---
