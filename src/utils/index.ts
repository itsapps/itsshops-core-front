import { slugify } from './slugify';
import { toIsoString } from './toIsoString';
import { map } from './map';

export {
  map,
  slugify,
  toIsoString
}

export default {
  map,
  slugify,
  toIsoString
}